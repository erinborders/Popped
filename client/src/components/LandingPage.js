import React, { Component } from 'react'

export default class LandingPage extends Component {
    render() {
        return (
            <div>
                <div id="home-container">
                    <div id="inner">
                        {/* Popped logo */}
                        <p class="subtitle">All pop ups in one place</p>
                    </div>
                    <div id="animation">
                        {/* circular gradient expanding outward animation */}
                    </div>
                </div>
            </div>
        )
    }
}
